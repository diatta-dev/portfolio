# Git ne reconnaît les commentaires qu'en début de ligne :
# un « # » placé après un motif en fait partie et casse la règle.

# ── outils locaux ──────────────────────────────────────────
# configuration de l'assistant, propre à ce poste
.claude/
# créé par la CLI Vercel, contient des identifiants
.vercel/
.idea/
.vscode/

# ── fichiers temporaires ───────────────────────────────────
# aperçus générés pendant le développement
_preview.html
*.log
*.tmp
*~

# ── systèmes d'exploitation ────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── dépendances (si un outil de build est ajouté un jour) ──
node_modules/

# ── captures d'écran des projets ───────────────────────────
# Ne PAS ignorer assets/shots/ : sans les fichiers, les cartes projet
# afficheront « captures à venir » sur le site en ligne. Les WebP font
# 60 à 120 Ko pièce, il n'y a aucune raison de les tenir hors du dépôt.
# (Les vidéos de démonstration ont été remplacées par ces captures.)
